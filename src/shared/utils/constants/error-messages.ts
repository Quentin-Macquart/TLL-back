export enum ErrorMessages {
  //  Request
  GET = 'Sorry the data transfer did fail',
  POST_PUT = 'Sorry the data dit not update succesfully',
  DELETE = 'Sorry the data dit not delete succesfully',

  // Wrong param of query
  WRONG_PARAMETER = 'Sorry the data given in parameter is not compliant to the required parameter',
}
